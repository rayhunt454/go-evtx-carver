# go-evtx-carver
A Go library and CLI tool for parsing Windows Event Logs (EVTX) and carving EVTX chunks/records directly from raw images (disk dumps, memory dumps, page files)—useful when a complete, valid `.evtx` file is unavailable. From the PT ESC IR team.
